{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix ../shared-types && npm install --prefix ../admin-backend && npm --prefix ../admin-backend run build && npm install",
  "buildCommand": "npm run build",
  "framework": "nextjs"
}
